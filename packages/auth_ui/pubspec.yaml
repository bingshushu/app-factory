name: auth_ui
description: "Authentication UI components for App Factory"
version: 0.0.1
resolution: workspace

environment:
  sdk: ^3.8.0
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter

  # 共享包
  core:
  api_client:
  shared_models:

  # 状态管理
  flutter_riverpod: ^3.2.0
  riverpod_annotation: ^4.0.1

  # 路由
  go_router: ^17.0.1

dev_dependencies:
  flutter_lints: ^6.0.0
  riverpod_generator: ^4.0.2
  build_runner: ^2.10.5
